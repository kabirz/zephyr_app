# Keep the help strings in sync with the values in the .py files!
west-commands:
  - file: scripts/west_commands/archive.py
    commands:
      - name: archive
        class: Archive
        help: archive all output images
